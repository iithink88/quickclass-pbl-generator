---
name: quickclass-pbl-generator
description: 为 QuickClass 课堂生成 PBL（项目式学习）完整资源包。当用户要求"生成某某 PBL""做某某 PBL 项目，在 QuickClass 上课/用"，或需要一套含"科普文章 + 基础知识 + 真实案例 + 数据记录表"的 PBL 配套资源时使用。产出 5 个统一排版的 HTML 文件（首页导航 + 约500字科普文章 + 探究工具卡 + 3个联网核实的真实案例 + 可打印 A4 数据记录表），并给出 QuickClass 课前/课中/课后用法与五维评价。参考成品：校园噪音调查（六年级）、班级气象站（初中·信息科技+数学+语文）。
agent_created: true
---

# QuickClass PBL 资源包生成器

## 用途
为「在 QuickClass 上落地」的项目式学习（PBL）一键生成一套统一排版的配套资源包。每个包含 5 个 HTML 文件：首页导航 + 约 500 字科普文章 + 探究工具卡（基础知识）+ 3 个联网核实的真实案例 + 可打印 A4 数据记录表，并附 QuickClass 课前/课中/课后用法。

## 何时使用
- 用户说「生成某某的 PBL」「做一个某某 PBL 项目，在 QuickClass 上用/上课」。
- 用户需要一套含「科普文章 + 基础知识 + 真实案例 + 数据记录表」的 PBL 配套资源。
- 参考成品：校园噪音调查（六年级）、班级气象站（初中·信息科技+数学+语文）。遇到同类「QuickClass + PBL」备课需求即触发。

## 工作流
1. **解析需求**：从用户消息提取 项目主题、适用年级、学科组合（如 信息科技+数学+语文）、是否有参考文件（.docx/.pdf/.txt 含案例与 QuickClass 流程）。
2. **读取参考文件**（若有）：用 Python 解析 docx（unzip→`word/document.xml` 取 `<w:t>`）/ pdf（`pypdf`）/ txt，抽取驱动性问题、分组方式、QuickClass 用法示例。无参考文件则依据主题自行设计。
3. **设计项目骨架**：
   - 提炼一句「驱动性问题」（开放式、可行动）。
   - 若多学科，给出分组分工（如 数据获取 / 数据处理 / 前端展示 三组）。
   - 固定四阶段：① 设计项目 ② 准备资源 ③ 课堂探究 ④ 评价反思。
4. **联网核实 3 个真实案例**（关键，绝不编造）：用 WebSearch 查 国内 + 国际 各至少 1 个真实学校/项目案例，记录来源年份与可引用事实；优先覆盖「学生探究型 / 科技型 / 设计型」三种思路，便于学生借鉴方法。
5. **撰写 5 页内容**（版式与字数规则见 `references/design-system.md`）：
   - 资源1 科普文章：约 500 字（汉字 400–600），学生适读，无生僻术语。
   - 资源2 基础知识/工具卡：本主题核心概念 + 简易测量/获取方法 + 安全规范提示。
   - 资源3 案例：3 张案例卡（标签 + 简述 + 来源）。
   - 资源4 数据记录表：A4 版，含空白记录行 + 总结反思区；对比型项目加「预报 vs 实测」等对照列。
   - 首页 index：驱动性问题 + 分组 + 四阶段 + 资源卡片 + QuickClass 用法（课前摸底三问 / 课中苏格拉底对话 / 成果提交 / 五维评价）。
   - 学生页可选加「老师提示」框（视觉标记为老师专用，不影响学生阅读）。
6. **生成 HTML + Markdown**：用 `scripts/build_pack.py` 读取 spec JSON，一次产出两套资源（推荐，保证排版一致）：
   - `桌面/<项目名>PBL资源/` 下：`index.html` + `01~04_*.html`（课堂展示 / 打印）
   - `桌面/<项目名>PBL资源/md/` 下：`00_首页导航.md` + `01~04_*.md`（上传 QuickClass 知识库用）
   也可按 design-system.md 的 CSS 手写 HTML；Markdown 由脚本自动从 HTML 转换，数据记录表（资源4）用 spec 里的 `body_md` 字段直接提供。
7. **自检**：脚本会打印资源1 汉字数；确认在 400–620 之间、案例均有出处、数据表可打印（带打印按钮）。
8. **呈现**：用 present_files 打开 `index.html` 并列出 5 个文件。

## 设计规则
- 配色：每个项目用一种主题色（accent）与浅色底（soft），如 噪音=蓝 `#2563eb`/`#eff6ff`、气象=青 `#0ea5e9`/`#ecfeff`。
- 每个资源包同时产出 HTML（课堂展示 / 打印）与 Markdown（`md/` 子目录，上传 QuickClass 知识库用）。数据记录表（资源4）在 spec 里用 `body_md` 提供 Markdown 表格（HTML 用 JS 填行，自动转换会得到空表）。
- 所有页面 `@media print` 友好；数据表页带「打印 / 另存为 PDF」按钮。
- QuickClass 五维评价固定为：问题清晰度 / 数据完整性 / 方案可行性 / 协作 / 展示。
- 案例务必真实可查；若某主题实在难找，至少保证 2 个真实 + 1 个「启发型改编」并明确注明。

## spec JSON 示例（传给 build_pack.py）
```json
{
  "title": "校园噪音调查",
  "meta": "适用：六年级 ｜ 设计：WorkBuddy（备课参谋） ｜ 落地：QuickClass",
  "driving_question": "我们校园里哪里最吵？噪音怎样影响健康？能用测量+创意让校园更安静吗？",
  "accent": "#2563eb", "soft": "#eff6ff",
  "groups": [
    {"name":"测量组","duty":"用 App 按规范测分贝并记录"},
    {"name":"分析组","duty":"整理数据、画图表、找最吵点"},
    {"name":"方案组","duty":"借鉴案例设计降噪办法"}
  ],
  "phases": ["① 设计项目","② 准备资源","③ 课堂探究","④ 评价反思"],
  "quickclass_usage": "<ul><li><b>课前：</b>发资源1作预习，AI 批改并摸底。</li></ul>",
  "note_text": "本文档由 WorkBuddy 按双引擎方法论生成，案例来自公开报道，可放心引用。",
  "out_dir": "C:/Users/lenovo/Desktop/校园噪音调查PBL资源",
  "check_article": true,
  "resources": [
    {"file":"01_噪音科普文章.html","num":"资源 1","kind":"课前阅读","title":"噪音污染科普文章",
     "meta":"六年级适读","body":"<p class=\"lead\">...</p>","teacher":"课前让学生圈出最吵地点"},
    {"file":"02_分贝测量基础知识.html","num":"资源 2","kind":"探究工具","title":"分贝测量基础",
     "meta":"课中探究卡","body":"...","teacher":""},
    {"file":"03_国内外降噪案例.html","num":"资源 3","kind":"案例探究","title":"降噪案例（3个）",
     "meta":"课堂讨论","body":"<div class=\"case\">...</div>"},
    {"file":"04_噪音数据记录表.html","num":"资源 4","kind":"数据模板","title":"噪音数据记录表",
     "meta":"打印版 A4","body":"<table>...</table>","print":true,
     "body_md":"## 测量记录\n\n| 序号 | 地点 | 分贝(dB) | 备注 |\n|:----:|:----:|:--------:|:----:|\n| 1 |  |  |  |\n| 2 |  |  |  |\n\n## 总结\n1. 最吵的地方：____。\n2. 我们的降噪建议：____。"}
  ]
}
```

## 配套资源
- `references/design-system.md`：共享 CSS、每页版式规范、字数自检方法。
- `scripts/build_pack.py`：spec JSON → 5 个 HTML（index 卡片自动生成、文章字数自检）。
